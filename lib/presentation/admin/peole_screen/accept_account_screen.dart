import 'package:flutter/material.dart';

class AcceptAccountScreen extends StatelessWidget {
  const AcceptAccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}